# sudo: required
# language: java
# jdk: oraclejdk11

# services:
#   - postgresql

# before_install:
#   - cd backend
#   - chmod +x mvnw

# script:
#   - ./mvnw clean install -DskipTests=false -B

# dist: trusty

# deploy:
#   provider: heroku
#   api_key: $HEROKU_API_KEY
#   on:
#     all_branches: true
#   app: pharmacy-tim9


sudo: required
language: node_js

before_install:
  - cd frontend
  
deploy:
  provider: heroku
  api_key: $HEROKU_API_KEY
  on:
    all_branches: true
  app: pharmacy9
